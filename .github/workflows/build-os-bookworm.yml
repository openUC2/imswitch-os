---
name: build-os-bookworm
on:
  push:
    branches:
      - 'main'
    tags:
      - 'v*'
  pull_request:
  merge_group:
  workflow_dispatch:
    inputs:
      git-ref:
        description: 'Git ref (optional)'
        required: false

jobs:

  build:
    name: build
    strategy:
      fail-fast: false
      matrix:
        base_variant_name:
          - lite
    permissions:
      contents: read
      packages: write
      id-token: write
    uses: ./.github/workflows/build-os.yml
    secrets: inherit
    with:
      name: rpi-uc2
      base_release_name: bookworm
      base_image_variant: ${{ matrix.base_variant_name }}
      base_release_date: 2024-11-19
      arch: arm64

  upload-zenodo:
    name: Upload to Zenodo
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: ${{ needs.build.outputs.image_name }}
          path: ./

      - name: Unzip artifact
        run: unzip "${{ needs.build.outputs.image_name }}.zip"

      - name: Create Zenodo deposit
        id: create_deposit
        env:
          ZENODO_API: "https://zenodo.org/api/deposit/depositions"
        run: |
          RESPONSE=$(curl -s -X POST "$ZENODO_API" \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer ${{ secrets.ZENODO_ACCESS_TOKEN }}" \
            --data '{
              "metadata": {
                "title": "Raspberry Pi OS Bookworm",
                "upload_type": "dataset",
                "description": "Automated build of Raspberry Pi OS (Bookworm) for UC2",
                "creators": [{"name": "Benedict Diederich"}]
              }
            }')

          DEPOSIT_ID=$(echo $RESPONSE | jq -r '.id')
          echo "DEPOSIT_ID=$DEPOSIT_ID" >> $GITHUB_ENV

      - name: Upload image to Zenodo
        run: |
          ZENODO_API="https://zenodo.org/api/deposit/depositions/$DEPOSIT_ID/files"
          FILENAME="${{ needs.build.outputs.image_name }}.img.xz"
          curl -s -X POST "$ZENODO_API" \
            -H "Authorization: Bearer ${{ secrets.ZENODO_ACCESS_TOKEN }}" \
            -F "file=@$FILENAME"

      - name: Publish Zenodo record
        run: |
          curl -s -X POST "https://zenodo.org/api/deposit/depositions/$DEPOSIT_ID/actions/publish" \
            -H "Authorization: Bearer ${{ secrets.ZENODO_ACCESS_TOKEN }}"

      - name: Output Zenodo link
        run: echo "Zenodo record available at https://zenodo.org/record/$DEPOSIT_ID"
